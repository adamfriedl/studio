org: adamfriedl
defaults:
  branchPrefix: studio/
  draftPR: true
templates: {}
repos:
  - name: pad-lab
    # owner defaults to org
  - name: studio
    # self-target: allowlist / harness changes via draft PR (merge carefully)